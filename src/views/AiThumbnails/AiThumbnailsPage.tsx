'use client';

/**
 * AI Thumbnail Generator — page view (placeholder).
 * The full UI will be implemented in Phase 2.
 * Backend (tRPC router) is fully functional.
 */
export function AiThumbnailsPage() {
  return (
    <div style={{ padding: 40, textAlign: 'center', opacity: 0.6 }}>
      <h1>AI Thumbnail Generator</h1>
      <p>Coming soon — backend is ready.</p>
    </div>
  );
}
